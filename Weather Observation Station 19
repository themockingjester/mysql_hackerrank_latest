/*
Enter your query here.
*/
SELECT ROUND(POWER((POWER(((SELECT MAX(LAT_N) FROM  STATION) - (SELECT MIN(LAT_N) FROM  STATION)),2) + POWER(((SELECT MAX(LONG_W) FROM  STATION) - (SELECT MIN(LONG_W) FROM  STATION)),2)),0.5),4);
